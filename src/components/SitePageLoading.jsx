export default function SitePageLoading() {
  return (
    <div
      style={{
        padding: "48px 32px",
        color: "var(--color-text-muted)",
        fontSize: "14px",
      }}
    >
      Loading…
    </div>
  );
}
